name: "[PR] Validate"
on:
  pull_request:
    branches:
      - "*"
jobs:
  run-pr-validation:
    name: Validate PR
    uses: fluidfoslex/sf-workflows/blob/main/.github/workflows/validate.yml
    permissions:
      contents: read
    secrets: inherit
